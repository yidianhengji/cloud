<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.dao.ProductStockMapper">

    <resultMap type="com.cloud.entity.ProductStock" id="ProductStockMap">
        <result property="uuid" column="uuid" jdbcType="VARCHAR"/>
        <result property="combinationId" column="combination_id" jdbcType="VARCHAR"/>
        <result property="productId" column="product_id" jdbcType="VARCHAR"/>
        <result property="modifyTime" column="modify_time" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProductStockMap">
        select
          uuid, combination_id, product_id, modify_time, create_time
        from cloudcity.tb_product_stock
        <where>
            <if test="uuid != null and uuid != ''">
                and uuid = #{uuid}
            </if>
        </where>
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProductStockMap">
        select
        uuid, combination_id, product_id, modify_time, create_time
        from cloudcity.tb_product_stock
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProductStockMap">
        select
          uuid, combination_id, product_id, modify_time, create_time
        from cloudcity.tb_product_stock
        <where>
            <if test="uuid != null and uuid != ''">
                and uuid = #{uuid}
            </if>
            <if test="combinationId != null and combinationId != ''">
                and combination_id = #{combinationId}
            </if>
            <if test="productId != null and productId != ''">
                and product_id = #{productId}
            </if>
            <if test="modifyTime != null">
                and modify_time = #{modifyTime}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert">
        insert into  cloudcity.tb_product_stock(uuid,combination_id, product_id, modify_time, create_time)
        values (#{uuid},#{combinationId}, #{productId}, #{modifyTime}, #{createTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update cloudcity.tb_product_stock
        <set>
            <if test="combinationId != null and combinationId != ''">
                combination_id = #{combinationId},
            </if>
            <if test="productId != null and productId != ''">
                product_id = #{productId},
            </if>
            <if test="modifyTime != null">
                modify_time = #{modifyTime},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where uuid = #{uuid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from cloudcity.tb_product_stock where uuid = #{uuid}
    </delete>

</mapper>